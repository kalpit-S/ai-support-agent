# api/requirements.txt
# Python dependencies for the API service.
# Install with: pip install -r requirements.txt

# ============================================================
# WEB FRAMEWORK
# ============================================================
fastapi>=0.100.0          # Modern async web framework, auto-generates OpenAPI docs
uvicorn>=0.23.0           # ASGI server to run FastAPI (like gunicorn but for async)

# ============================================================
# DATABASE
# ============================================================
sqlalchemy>=2.0.0         # ORM - maps Python classes to database tables
psycopg2-binary>=2.9.0    # PostgreSQL driver (binary = no compile needed)

# ============================================================
# REDIS
# ============================================================
redis>=4.0.0              # Redis client for message batching

# ============================================================
# VALIDATION & SERIALIZATION
# ============================================================
pydantic>=2.0.0           # Data validation (FastAPI uses this for request/response models)
pydantic-settings>=2.0.0  # Load settings from environment variables

# ============================================================
# UTILITIES
# ============================================================
python-dotenv>=1.0.0      # Load .env files (optional, for local dev without Docker)

# ============================================================
# VOICE (Real-time STT/TTS + LLM)
# ============================================================
deepgram-sdk>=3.0.0       # Deepgram STT (nova-3) and TTS (aura-2) streaming
google-genai>=1.0.0       # Google Gemini API (GA library) for voice LLM
